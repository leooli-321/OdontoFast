<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OdontoFast"
        tools:targetApi="31">

        <!-- BoasVindasActivity será a primeira página -->
        <activity
            android:name=".BoasVindasActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Outras activities (Agenda, Checklist, Notificações, etc.) -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".FichaActivity" />
        <activity android:name=".AgendaActivity" />
        <activity android:name=".ChecklistActivity" />
        <activity android:name=".NotificationsActivity" />

    </application>

</manifest>
